import Link from "next/link";

const Logo = () => {
  return (
    <Link href="/">
      <p className="font-bold text-3xl select-none uppercase tracking-wider">
        ctrl
      </p>
    </Link>
  );
};

export default Logo;
